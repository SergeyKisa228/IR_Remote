# IR_Remote
STM32F401 reads IR signals (NEC) via KY-022. TIM2 Input Capture measures pulses; interrupts decode data. Main loop shows button name on 1.8" ST7735 SPI display. Demonstrates SPI, GPIO, timers, interrupts in CubeIDE/HAL.
